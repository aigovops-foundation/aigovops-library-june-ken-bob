// src/core/lantern.js
// LANTERN COMPILER — frameworks → controls.
// The library + compiler now live in the single source of truth so the Node core
// and the browser demo can never drift. This file preserves the public API.
export { LIBRARY, compile, frameworks } from './yesgate.shared.js';
