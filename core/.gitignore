node_modules/
.env
keys/*.pem
ledger/*.ndjson
*.log
.DS_Store

# secret broker backing store (FileProvider) — NEVER commit
secrets.local.json

# generated artifacts (rebuilt by npm run sbom / release / export:evidence / report:compliance)
release/
evidence-bundle/
sbom.cdx.json
compliance/
